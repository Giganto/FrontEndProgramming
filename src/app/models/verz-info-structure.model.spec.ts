import { VerzInfoStructure } from './verz-info-structure.model';

describe('VerzInfoStructure', () => {
  it('should create an instance', () => {
    expect(new VerzInfoStructure()).toBeTruthy();
  });
});
